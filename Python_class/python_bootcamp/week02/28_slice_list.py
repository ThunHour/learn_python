def slice_list(givenList,index):

    # frontRemove = givenList[0:index]
    # reverse_list = givenList[::-1]
    # backRemove = reverse_list[0:index]
    #
    # for i in frontRemove:
    #     givenList.remove(i)
    # for j in backRemove:
    #     givenList.remove(j)
    #
    # print(givenList)
    # return


print(slice_list ([50,10,62,32,64,78,90], 2))
